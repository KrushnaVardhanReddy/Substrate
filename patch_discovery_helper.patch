--- api/internal/services/discovery_helper.go
+++ api/internal/services/discovery_helper.go
@@ -37,7 +37,7 @@
			continue
		}

-		contractID, err := store.UpsertContract(ctx, providerRepoID, "unknown", "discovered", req.CommitSHA, req.CommitSHA, edge.TargetURL)
+		contractID, err := store.UpsertContract(ctx, providerRepoID, "unknown", "discovered", "unknown", "unknown", edge.TargetURL)
		if err != nil {
			continue
		}
