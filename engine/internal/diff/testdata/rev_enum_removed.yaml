openapi: 3.0.0
info:
  title: Test
  version: 1.0.0
paths:
  /test:
    get:
      parameters:
        - name: sort
          in: query
          schema:
            type: string
            enum: [asc]
      responses:
        '200':
          description: ok
