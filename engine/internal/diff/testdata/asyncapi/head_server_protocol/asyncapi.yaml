asyncapi: "2.6.0"
info:
  title: User Events
  version: "1.0.0"
servers:
  production:
    url: mqtt://broker:1883
    protocol: mqtt
channels:
  user/signedup:
    subscribe:
      message:
        payload:
          type: object
          required: [id, email]
          properties:
            id: { type: string }
            email: { type: string }
