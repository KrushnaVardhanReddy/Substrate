asyncapi: "2.6.0"
info:
  title: User Events
  version: "1.0.0"
channels:
  user/signedup:
    subscribe:
      message:
        payload:
          type: object
          required: [id]
          properties:
            id: { type: string }
