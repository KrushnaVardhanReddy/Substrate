service: mock-service
schema_type: openapi
spec_path: base.yaml
overrides:
  - rule_id: ENDPOINT_REMOVED
    path: GET /users
    reason: "Because tests require it to pass and this is a mock config with a minimum of 20 chars"
    approved_by: jules@test.com
    expires: 2030-01-01
  - rule_id: ENDPOINT_REMOVED
    path: GET /deleted
    reason: "Because tests require it to pass and this is a mock config with a minimum of 20 chars"
    approved_by: jules@test.com
    expires: 2030-01-01
