# Phase 16 - Task 99: E2E Validation (No Mocks)

## 1. Objective
Validate all Phase 16 documentation portal features end-to-end using real Go API tests and Playwright UI tests.

## 2. Database Seed
```sql
INSERT INTO repos (org, repo, base_url) VALUES ('e2e-org', 'guide-api-repo', 'http://localhost:8081');
INSERT INTO repo_guides (org, repo, file_path, title, content)
  VALUES ('e2e-org', 'guide-api-repo', 'docs/authentication.md', 'Authentication Guide', '# Auth\nUse Bearer tokens.');
```

## 3. Go API Tests (`scripts/e2e/phase16_e2e_test.go`)
- `GET /api/v1/docs/e2e-org/guide-api-repo` — assert returns array with 1 guide.
- `GET /api/v1/docs/e2e-org/guide-api-repo/authentication` — assert returns guide content with correct title.
- `POST /api/v1/sandbox/token` — assert returns a short-lived PASETO token.
- `POST /api/v1/sandbox/request` (with valid token) — assert proxies the request and returns upstream response.
- `POST /api/v1/sandbox/request` (unauthenticated) — assert returns 401.

## 4. Playwright UI Tests (`dashboard/tests/phase16.spec.ts`)
- Navigate to the catalog page for `e2e-org/guide-api-repo`.
- Assert "Guides" tab is visible.
- Click "Guides" tab, assert "Authentication Guide" is listed and content renders.
- Click "Try It" on an endpoint, fill in a path param, click "Send", and assert a live response panel appears.
