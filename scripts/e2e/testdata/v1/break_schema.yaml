openapi: 3.0.0
info:
  title: Example API
  version: 1.0.0
paths:
  /users:
    get:
      summary: List users
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  # Breaking change: missing 'name' in required
                  required:
                    - id
                  properties:
                    id:
                      type: string
                    # Breaking change: missing 'name' property entirely or optional
