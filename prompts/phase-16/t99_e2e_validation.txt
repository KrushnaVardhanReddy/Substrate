TASK: P16-T99 — Phase 16 E2E Validation (No Mocks)

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Write comprehensive Go API tests and Playwright UI tests for all Phase 16 documentation portal features.

Spec (READ ONLY — implement from it, never edit):
  docs/specs/phase-16/p16-t99-e2e.md

═══════════════════════════════════════════════════════════════
CONSTRAINTS & RULES
═══════════════════════════════════════════════════════════════
- NO MOCKS for the API proxy or DB layer — use a real seeded Postgres instance.
- Playwright tests must assert actual DOM element IDs (`#guides-tab`, `#sandbox-response`).
- All Go tests must use `testify/assert` and be table-driven.

═══════════════════════════════════════════════════════════════
CONTEXT — EXISTING REPO LAYOUT & ARCHITECTURE
═══════════════════════════════════════════════════════════════
- IMPORTANT: Before writing code, view an existing E2E test (e.g., `scripts/e2e/phase15_e2e_test.go`) and mimic its exact setup, teardown, and assertion patterns. This will give you a major clue on how the test harness is wired up.
- Go E2E tests live in `scripts/e2e/`. Follow the pattern of `phase15_e2e_test.go`.
- Playwright tests live in `dashboard/tests/`. Follow the pattern of `dashboard.spec.ts`.
- DB seeding for E2E uses the helpers in `scripts/e2e/helpers.go` (`seedOrg`, `seedRepo`).
- The test database URL is read from `TEST_DATABASE_URL` environment variable.

═══════════════════════════════════════════════════════════════
IMPLEMENTATION TIPS
═══════════════════════════════════════════════════════════════
- Seed a `repo_guides` row directly via SQL in the test setup using `db.Exec()`.
- For the sandbox proxy E2E test, start a local `httptest.NewServer` that returns a known JSON response, and configure the test repo's `base_url` to point to it. This is NOT mocking — it is a real HTTP server.
- Playwright: wait for `#guides-tab` to be visible before clicking it, to avoid flaky race conditions.
- Add the new test file to `scripts/e2e/run_full_e2e.sh` so it is included in the `make e2e` run.

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════
1. FILE: scripts/e2e/phase16_e2e_test.go (CREATE)
2. FILE: dashboard/tests/phase16.spec.ts (CREATE)
3. MODIFY: scripts/e2e/run_full_e2e.sh — add phase16 test to the run list
4. Run `go test ./scripts/e2e/... -run TestPhase16` and `npx playwright test phase16`.

Commit: "jules: P16-T99 add Phase 16 E2E validation suite"
Target branch: feature/p16-t99-e2e
