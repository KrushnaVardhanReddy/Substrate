MANDATORY RULES — VIOLATION = REJECTED PR:
[safety rules prepended automatically by jules_submit.py]

---

TASK: P5-T07 — Dynamic Chaos & Scale Simulation

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Implement a massive-scale chaos engineering data generator to stress-test the Substrate API and UI. You will generate a parameterized number of mock repositories (including poison pills), fire them highly concurrently with jitter, mutate them over time, and output a detailed latency and error report.

Follow the exact architecture defined in the spec: `docs/specs/phase-5/p5-t07-scale-simulation.md`.

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════
1. **Scale Generator Script:** Create `scripts/e2e/scale_generator.go`. It must accept `--scale` and `--concurrency` flags.
2. **Cluster & Noise Gen:** 60% of repos are noise (including massive 50k-line schemas and infinite loops). 40% are strictly grouped into 10 distinct dependency clusters covering the 9 Substrate protocols.
3. **Phased Execution:** 
   - Flood the API concurrently (with jitter).
   - Mutate the API (delete/update repos) to test graph recalculation.
   - Assert the graph is perfectly accurate.
4. **Reporting Matrix:** The script must aggregate the results and print a terminal table showing Average Latency per Protocol, HTTP status codes, and caught panics.
5. **Makefile Target:** Update `Makefile` to add an `e2e-scale` command that executes this script with `--scale=100`.

═══════════════════════════════════════════════════════════════
FILES LIST
═══════════════════════════════════════════════════════════════
FILES TO ADD:
- scripts/e2e/scale_generator.go

FILES TO MODIFY:
- Makefile

Commit: "jules: implement P5-T07 dynamic chaos simulation with observability reporting"
