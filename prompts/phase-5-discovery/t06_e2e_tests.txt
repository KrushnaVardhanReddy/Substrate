TASK: P5-T06 — Phase 5 Discovery Scanners Full-Stack E2E Validation

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Using the new Full-Stack PGlite Test Harness (CC-T02), implement the complete E2E 
test suite for the Phase 5 Discovery Scanners. 

You must write BOTH the Go API tests (to assert the scanner engine and confidence scoring) 
AND the Playwright UI tests (to ensure the discovery UI renders scans correctly).

═══════════════════════════════════════════════════════════════
CRITICAL READING — DO THIS BEFORE WRITING A SINGLE LINE OF CODE
═══════════════════════════════════════════════════════════════
1. docs/specs/e2e/phase5-discovery.md
   → Read the exact API assertions and UI actions required.

2. dashboard/src/routes/(app)/org/[org]/discovery/[repo]/+page.svelte
   → Review the DOM structure of the Discovery Dashboard. Note the trigger mechanism 
     for the "Run Full Scan" button.

3. seed_mcp.sql
   → Ensure the discovery test repo `mcp-org/discovery-test-repo` is seeded.

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════
1. FILE: scripts/e2e/phase5_api_test.go (NEW)
   - Assert POST `/api/v1/discovery/scan/{org}/{repo}` triggers the async job.
   - Assert GET `/api/v1/discovery/results/{org}/{repo}` returns the mocked dependencies.

2. FILE: dashboard/playwright/discovery.spec.ts (NEW)
   - Navigate to `/org/mcp-org/discovery/mcp-org/discovery-test-repo`.
   - Click "Run Full Scan".
   - Assert the list populates with Env Vars and Terraform nodes.
   - Click a node to open the Confidence Score panel.

Commit message: "test: P5-T06 add Phase 5 full-stack E2E tests (Discovery Scanners)"
Target branch: feature/dev
