MANDATORY RULES — VIOLATION = REJECTED PR:
1. NEVER stub, mock, or TODO existing implementation code.
2. Every file you modify MUST still build — run `go build ./...` and `go vet ./...` before committing.
3. If a test fails, FIX the code or test — do NOT delete or skip tests.
4. Do NOT alter any file in docs/specs/ — those are READ-ONLY source-of-truth specs.
5. TESTING IS MANDATORY — every task MUST include both:
   a. Unit tests: table-driven `_test.go` files. Min 80% coverage on new code.
   b. E2E tests: invoke the compiled binary with real input files and assert stdout/exit code.
6. Commit message must start with "jules: " prefix.
7. 100% SPEC-FIRST RULE: Implement exactly what docs/specs/diff-report-schema.md defines.

Project: Substrate — Go Diff Engine CLI
Tech stack: Go 1.22, standard library + oasdiff (Apache 2.0 open-source library)
Key decision: OpenAPI diffing is handled by github.com/oasdiff/oasdiff — do NOT reimplement diff logic.

---

TASK: P1-T01 — Go Module Scaffold + oasdiff Dependency

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Scaffold the Go module for the Substrate diff engine.
Add oasdiff as the core OpenAPI diff library dependency.
Define the DiffReport structs that all future tasks will use.
No business diff logic yet — only the skeleton and data types.

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════

1. FILE: engine/go.mod
   - Module name: github.com/KrushnaVardhanReddy/substrate/engine
   - Go version: 1.22
   - Dependencies to add (run `go get`):
     * github.com/oasdiff/oasdiff (latest)
     * github.com/spf13/cobra (for CLI flag parsing)

2. FILE: engine/cmd/substrate/main.go
   - Entry point for the CLI binary.
   - Print "Substrate v0.1.0 — powered by oasdiff" and exit 0 for now.
   - Use cobra to define a root command with placeholder subcommands: `diff`, `validate`.

3. FILE: engine/internal/report/report.go
   - Define Go structs that EXACTLY match the DiffReport JSON schema in docs/specs/diff-report-schema.md.
   - Every field must have correct `json:"..."` tags matching the spec field names exactly.
   - Structs to define:
     * DiffReport
     * Summary
     * Change
   - Type aliases:
     * SchemaType (string) — values: "openapi", "sql", "graphql", "protobuf", "avro"
     * Severity (string) — values: "BREAKING", "WARNING", "SAFE", "NO_CHANGES"
     * ChangeSeverity (string) — values: "BREAKING", "WARNING", "SAFE"
   - Constants for every enum value listed above.

4. FILE: engine/internal/report/report_test.go
   - Table-driven tests:
     a. Marshal a sample DiffReport (matching the complete example in docs/specs/diff-report-schema.md) to JSON and verify all field names and values match exactly.
     b. Unmarshal that same JSON back to a DiffReport struct and verify round-trip integrity.

5. FILE: engine/Makefile
   - Targets: build, test, lint, clean, deps
   - `deps`: go mod tidy && go mod download
   - `build`: go build -o bin/substrate ./cmd/substrate
   - `test`: go test ./...
   - `lint`: go vet ./...
   - `clean`: rm -rf bin/

6. FILE: .github/workflows/engine-ci.yml (at repo root)
   - Trigger: pull_request targeting main, path filter: engine/**
   - Steps: checkout, setup-go@v5, `make -C engine deps`, `make -C engine test`, `make -C engine lint`
   - Block merge on any failure.

═══════════════════════════════════════════════════════════════
FILES LIST
═══════════════════════════════════════════════════════════════

FILES TO ADD:
- engine/go.mod
- engine/go.sum
- engine/cmd/substrate/main.go
- engine/internal/report/report.go
- engine/internal/report/report_test.go
- engine/Makefile
- .github/workflows/engine-ci.yml

FILES TO MODIFY:
- None

Commit: "jules: scaffold Go diff engine module with oasdiff dependency and DiffReport structs"

