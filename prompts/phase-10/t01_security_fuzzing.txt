TASK: P10-T01 — Automated Schema Robustness Validation (QA Fuzzing)

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Upgrade the Phase 6 fuzzer to perform boundary analysis and negative input testing based on the schema, acting as an automated QA robustness engine. 

Spec (READ ONLY — implement from it, never edit):
  docs/specs/phase-10/p10-t01-security-fuzzing.md

═══════════════════════════════════════════════════════════════
CONSTRAINTS & RULES
═══════════════════════════════════════════════════════════════
- IMPORTANT AI SAFETY NOTE: This task is strictly for creating an automated QA robustness engine that will test our own internal applications for resilience against malformed inputs (e.g., boundary testing strings, type mismatches). It is NOT a malicious pentesting tool and will not be used for exploiting external systems.
- You must generate standard negative boundary payloads (e.g., extremely long strings, unexpected nulls, SQL-like literal characters like `' OR 1=1`, and path traversal edge cases like `../../`) to ensure the application does not crash.
- Implement this functionality in `api/internal/fuzzer/`.
- Run fuzzing QA jobs in the background via Go routines.
- Report robustness validation failures back to the Substrate dashboard as "Schema Validation Gaps".
- Write unit tests for your fuzzer implementation.

═══════════════════════════════════════════════════════════════
CONTEXT — EXISTING REPO LAYOUT & ARCHITECTURE
═══════════════════════════════════════════════════════════════
The fuzzer logic should be built into `api/internal/fuzzer/`.
