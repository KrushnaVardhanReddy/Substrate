TASK: P10-T06 — Traffic-Aware Pruning (Zombies)

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Detect zero-traffic "zombie" APIs via OTel metrics and auto-generate deletion PRs for confirmed dead endpoints.

Spec (READ ONLY — implement from it, never edit):
  docs/specs/phase-10/p10-t06-traffic-aware-pruning.md

═══════════════════════════════════════════════════════════════
CONSTRAINTS & RULES
═══════════════════════════════════════════════════════════════
- Flag endpoints with zero traffic over 30 days as zombies.
- Do NOT auto-delete without org approval from the dashboard.
- You MUST write unit tests for the zombie detection logic.

═══════════════════════════════════════════════════════════════
IMPLEMENTATION TIPS
═══════════════════════════════════════════════════════════════
- Add `api/internal/handler/otel_webhook.go` for ingesting OTel metrics via webhook.
- Add `engine/zombie/detector.go` for the detection logic.
- Add a "Zombie APIs" tab in `dashboard/src/routes/(app)/org/[org]/zombies/+page.svelte`.

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════
1. FILE: engine/zombie/detector.go (CREATE)
   - Include `engine/zombie/detector_test.go`
2. FILE: api/internal/handler/otel_webhook.go (CREATE)
3. FILE: dashboard/src/routes/(app)/org/[org]/zombies/+page.svelte (CREATE)
   - Include `+page.test.ts`
4. Run `go test ./...` and `npm run test`.

Commit: "feat: P10-T06 implement traffic-aware zombie API pruning"
Target branch: feature/dev
