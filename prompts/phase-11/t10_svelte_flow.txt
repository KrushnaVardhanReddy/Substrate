TASK: P11-T10 — Svelte Flow Migration

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Migrate the primary dependency graph visualization in the dashboard from `cytoscape.js` to `@xyflow/svelte` (Svelte Flow). This allows native Svelte reactivity, gorgeous HTML custom nodes, and built-in minimap support.

Spec (READ ONLY — implement from it, never edit):
  docs/specs/phase-11/p11-t10-svelte-flow.md

═══════════════════════════════════════════════════════════════
CONSTRAINTS & RULES
═══════════════════════════════════════════════════════════════
- Do NOT use Cytoscape.js or any of its dependencies anymore.
- A static HTML/CSS mockup for the Service Node component has been generated by Stitch and is located at: {{MOCKUP_FILE_PATH}}
- You must port the design from the mockup into a reactive Svelte component.

═══════════════════════════════════════════════════════════════
CONTEXT — EXISTING REPO LAYOUT & ARCHITECTURE
═══════════════════════════════════════════════════════════════
The SvelteKit dashboard lives in `dashboard/`.
The main graph view is located in `dashboard/src/routes/graph/+page.svelte` or the dashboard root.
The graph data is fetched from the Go API (`GET /api/v1/graph`).
You will need to map the API's graph structure (nodes and edges) into the specific data format expected by Svelte Flow.

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════

1. MODIFY: package.json
   Remove cytoscape and add `@xyflow/svelte`.

2. FILE: dashboard/src/lib/components/ServiceNode.svelte (CREATE NEW)
   Implement the custom Node component based on the Stitch mockup. Display service name, health status, and language badge.

3. MODIFY: dashboard/src/routes/graph/+page.svelte (or main graph view)
   Initialize `<SvelteFlow>` using the nodes and edges from the API.
   Integrate the `<MiniMap />`, `<Controls />`, and `<Background />` components.

═══════════════════════════════════════════════════════════════
FILES LIST
═══════════════════════════════════════════════════════════════

FILES TO CREATE:
- dashboard/src/lib/components/ServiceNode.svelte

FILES TO MODIFY:
- dashboard/package.json
- dashboard/src/routes/graph/+page.svelte (or main view)

Commit: "feat: P11-T10 migrate graph visualization to svelte flow"
Target branch: feature/dev
