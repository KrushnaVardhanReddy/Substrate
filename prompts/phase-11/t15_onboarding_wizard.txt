TASK: P11-T15 — Zero-to-One Onboarding Wizard

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Create a frictionless, animated onboarding flow for first-time enterprise users. The flow must gracefully guide the user through: Connect GitHub -> Scan Repositories -> Build Graph.

Spec (READ ONLY — implement from it, never edit):
  docs/specs/phase-11/p11-t15-onboarding-wizard.md

═══════════════════════════════════════════════════════════════
CONSTRAINTS & RULES
═══════════════════════════════════════════════════════════════
- The onboarding wizard must use Svelte's built-in transition APIs (`fade`, `slide` or `crossfade`) to animate seamlessly between states.
- A static HTML/CSS mockup for the 3 wizard steps has been generated by Stitch and is located at: {{MOCKUP_FILE_PATH}}
- The final step must redirect to the main dashboard.

═══════════════════════════════════════════════════════════════
CONTEXT — EXISTING REPO LAYOUT & ARCHITECTURE
═══════════════════════════════════════════════════════════════
The SvelteKit dashboard lives in `dashboard/`.
The wizard should be implemented as a completely standalone route (`/onboarding`) so it doesn't clutter the main dashboard view.

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════

1. FILE: dashboard/src/routes/onboarding/+page.svelte (CREATE NEW)
   Port the HTML/CSS mockup into a multi-step Svelte component state machine.
   Step 1: "Connect GitHub" (Displays GitHub App installation link).
   Step 2: "Scanning Repositories" (Progress bar simulating the background tasks).
   Step 3: "Building Graph" (Animated graph construction preview).
   Final Step: "Enter Dashboard" button that uses `goto('/')` to redirect the user back to the main dashboard.

2. Ensure transitions between `step = 1`, `step = 2`, and `step = 3` are animated smoothly without layout jumping.

═══════════════════════════════════════════════════════════════
FILES LIST
═══════════════════════════════════════════════════════════════

FILES TO CREATE:
- dashboard/src/routes/onboarding/+page.svelte

Commit: "feat: P11-T15 implement zero to one onboarding wizard"
Target branch: feature/dev
