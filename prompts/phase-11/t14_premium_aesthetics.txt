TASK: P11-T14 — Premium Aesthetics System

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Overhaul the Substrate UI to align with top-tier Enterprise SaaS aesthetics. This includes a deep dark mode, glassmorphism overlays, curated HSL color palettes, and edge-flow micro-animations.

Spec (READ ONLY — implement from it, never edit):
  docs/specs/phase-11/p11-t14-premium-aesthetics.md

═══════════════════════════════════════════════════════════════
CONSTRAINTS & RULES
═══════════════════════════════════════════════════════════════
- A static HTML/CSS mockup of the new base layout and typography has been generated by Stitch and is located at: temp_mockups/t14_mockup.html
- Extract the CSS variables (e.g., deep dark mode colors, glassmorphism utilities) from the mockup.
- Do not fundamentally break the existing page routing or logical flow.

═══════════════════════════════════════════════════════════════
CONTEXT — EXISTING REPO LAYOUT & ARCHITECTURE
═══════════════════════════════════════════════════════════════
The SvelteKit dashboard lives in `dashboard/`.
Global CSS is defined in `dashboard/src/app.css` (or `global.css`).
The root layout is in `dashboard/src/routes/+layout.svelte`.
UI components use plain CSS or Tailwind (if configured).

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════

1. MODIFY: dashboard/src/app.css
   Extract and insert the core CSS variables and utility classes from the Stitch mockup.
   Implement the modern sans-serif typography constraints (e.g. Inter or Outfit).

2. MODIFY: dashboard/src/routes/+layout.svelte
   Apply the new background colors, font styles, and base structure to the app wrapper.
   Update primary navigation elements to use subtle hover/focus micro-animations.

3. COMPONENT UPDATES
   Ensure the new aesthetic seamlessly integrates into the SvelteKit frontend without breaking the existing layout structure of the graph view.

═══════════════════════════════════════════════════════════════
FILES LIST
═══════════════════════════════════════════════════════════════

FILES TO MODIFY:
- dashboard/src/app.css
- dashboard/src/routes/+layout.svelte

Commit: "feat: P11-T14 implement premium aesthetics system"
Target branch: feature/dev
