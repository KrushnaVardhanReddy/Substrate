MANDATORY RULES — VIOLATION = REJECTED PR:
[safety rules prepended automatically by jules_submit.py]

---

TASK: V1-T04 — Deployment Safety Gate (substrate check-deploy)

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Implement the CI/CD deployment safety gate to prevent bad deployments where a consumer microservice is deployed *before* its required provider. This command should run right before the CD step (e.g., `kubectl apply`).

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════
1. **CLI Command:** Add the `check-deploy` command to the Substrate CLI (`engine/cmd/substrate/main.go`).
2. **Registry API:** Add a `GET /api/v1/registry/can-deploy?repo=<name>&commit=<sha>` endpoint to the Go API server.
3. **Logic:** The API verifies all outbound dependencies of the `repo` at `commit`. It compares them against the current active versions of the providers in the registry. If the consumer requires a provider schema version that hasn't been deployed yet, return `HTTP 409 Conflict`.
4. **CLI Exit Codes:** The CLI must exit with `0` if safe, or non-zero (`1`) if blocked, which will fail the CI/CD pipeline.

═══════════════════════════════════════════════════════════════
FILES LIST
═══════════════════════════════════════════════════════════════
FILES TO ADD:
- engine/cmd/substrate/check_deploy.go

FILES TO MODIFY:
- engine/cmd/substrate/main.go
- api/internal/handlers/registry.go
- api/cmd/server/main.go

Commit: "jules: implement substrate check-deploy safety gate"
