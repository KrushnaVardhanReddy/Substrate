TASK: P-UI-ZOMBIE — Missing Playwright E2E Coverage for Zombies Dashboard

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Create a Playwright E2E test file for the Zombies Dashboard UI page (`/org/{org}/zombies`) to ensure it renders correctly and lists zombie APIs.

═══════════════════════════════════════════════════════════════
CRITICAL READING
═══════════════════════════════════════════════════════════════
1. dashboard/tests/e2e/graph.spec.ts
   → Study for standard Playwright setup and mocking patterns.
2. docs/specs/cross-cutting/p-ui-04-e2e-zombies.md
   → Read the test scenarios and constraints.

═══════════════════════════════════════════════════════════════
CONSTRAINTS & RULES
═══════════════════════════════════════════════════════════════
- New file: dashboard/tests/e2e/zombies.spec.ts
- Use the existing `playwright.config.ts` setup.
- Mock the backend API calls (`GET /api/v1/repos/*`, `GET /api/v1/zombies/*`, etc.) using `page.route()`.
- Add a suppression for dev-mode SSR `lifecycle_outside_component` crashes if necessary (e.g. `expect(true).toBe(true)`).

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════
1. FILE: dashboard/tests/e2e/zombies.spec.ts (NEW)
   Implement test scenarios for the Zombies Dashboard page.
2. Verify and test:
   - Run: cd dashboard && npx playwright test tests/e2e/zombies.spec.ts
   - Fix any errors before committing.

Commit message: "test: P-UI-ZOMBIE add Playwright E2E coverage for Zombies Dashboard"
Target branch: feature/dev
