TASK: P3-T12 — Phase 3 Contract Registry Full-Stack E2E Validation

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Using the new Full-Stack PGlite Test Harness (CC-T02), implement the complete E2E 
test suite for the Phase 3 Contract Registry. 

You must write BOTH the Go API tests (to assert graph traversal and safety gates) 
AND the Playwright UI tests (to ensure the graph renders correctly on the frontend).

═══════════════════════════════════════════════════════════════
CRITICAL READING — DO THIS BEFORE WRITING A SINGLE LINE OF CODE
═══════════════════════════════════════════════════════════════
1. docs/specs/e2e/phase3-graph.md
   → Read the exact API assertions and UI actions required.

2. dashboard/src/routes/(app)/org/[org]/graph/+page.svelte
   → Review the DOM structure. Note that the Cytoscape canvas is mounted inside a 
     div bound to `cyContainer`. You must assert the presence of nodes inside it.

3. seed_mcp.sql
   → Ensure the provider and consumer repo exist in the seed data so your tests 
     have graph nodes to render.

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════
1. FILE: scripts/e2e/phase3_api_test.go (NEW)
   - Read the `DATABASE_URL` env variable to connect to the PGlite instance.
   - Assert GET `/api/v1/graph/mcp-org` returns 200 OK and contains the `mcp-org/frontend` dependency.
   - Insert a breaking change into the database and assert GET `/api/v1/registry/can-deploy` returns 409 Conflict.

2. FILE: dashboard/playwright/graph.spec.ts (NEW)
   - Navigate to `/org/mcp-org/graph`.
   - Wait for the DOM and cytoscape elements to load.
   - Assert the nodes for backend and frontend are visible.
   - Click the backend node and assert the detail panel opens.

Commit message: "test: P3-T12 add Phase 3 full-stack E2E tests (API + UI)"
Target branch: feature/dev
