TASK: P7-T07 — Phase 7 Enterprise Full-Stack E2E Validation

═══════════════════════════════════════════════════════════════
OBJECTIVE
═══════════════════════════════════════════════════════════════
Using the new Full-Stack PGlite Test Harness (CC-T02), implement the complete E2E 
test suite for the Phase 7 Enterprise features (Webhooks, CEL Rules, Drift Sidecar). 

You must write BOTH the Go API tests (to assert rule evaluation and drift logs) 
AND the Playwright UI tests (to ensure the Enterprise dashboard operates correctly).

═══════════════════════════════════════════════════════════════
CRITICAL READING — DO THIS BEFORE WRITING A SINGLE LINE OF CODE
═══════════════════════════════════════════════════════════════
1. docs/specs/e2e/phase7-enterprise.md
   → Read the exact API assertions and UI actions required.

2. dashboard/src/routes/(app)/org/[org]/enterprise/+page.svelte
   → Review the DOM structure of the Enterprise Dashboard. Note the tab navigation 
     for Webhooks, Custom Rules, and Drift Detection.

3. seed_mcp.sql
   → Ensure the enterprise repo `mcp-org/enterprise-repo` and CEL rules are seeded.

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════
1. FILE: scripts/e2e/phase7_api_test.go (NEW)
   - Assert POST `/api/v1/enterprise/webhook` dispatches correctly.
   - Assert POST `/api/v1/enterprise/rules/validate` evaluates CEL rules correctly.
   - Assert GET `/api/v1/enterprise/drift/{org}/{repo}` returns drift reports.

2. FILE: dashboard/playwright/enterprise.spec.ts (NEW)
   - Navigate to `/org/mcp-org/enterprise`.
   - Click the "Custom Rules" tab, edit a rule, and click "Save Rule".
   - Navigate to "Drift Detection" tab and assert the report appears with a "Resolve" button.

Commit message: "test: P7-T07 add Phase 7 full-stack E2E tests (Enterprise Rollout)"
Target branch: feature/dev
